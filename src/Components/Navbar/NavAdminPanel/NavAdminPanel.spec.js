/*import { shallow } from 'enzyme';
import React from 'react';
import NavAdminPanel from './NavAdminPanel';

const props = {
    signOut(){},
}

const setUp = (props) => shallow(
<NavAdminPanel {...props} />
)

describe('NavAdminPanel component', () => {
    it('should render NavAdminPanel component', () => {
        const component = setUp(props);
        expect(component.debug()).toMatchSnapshot();
    });
});*/